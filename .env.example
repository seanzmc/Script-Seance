# Server-side only. Do not expose in client bundles.

# Google Gemini API key used for all AI generation requests.
GEMINI_API_KEY=

# Inworld TTS API key (required for inworld/dual provider modes).
INWORLD_API_KEY=

# Inworld API secret used to mint JWTs server-side.
INWORLD_API_SECRET=

# Inworld workspace id used for custom voice catalog requests.
INWORLD_WORKSPACE_ID=

# Admin password required to log in and access /api/ai/* endpoints.
ADMIN_PASSWORD=

# Optional API server port (defaults to 3001).
PORT=3001

# Environment setting to control cookie security (use "production" for HTTPS-only cookies).
NODE_ENV=development

# Trust exactly one upstream proxy for forwarded client IPs (set to 1 behind a trusted reverse proxy).
TRUST_PROXY=0

# AI rate limits: requests per minute and per day (per session, IP fallback).
AI_RPM=30
AI_RPD=500

# TTS provider mode: gemini | inworld | dual (dual prefers inworld with gemini fallback).
TTS_PROVIDER=dual

# Inworld TTS model for speech generation.
TTS_INWORLD_MODEL=inworld-tts-1.5-max

# Optional inworld API base override (defaults to https://api.inworld.ai).
INWORLD_API_BASE=https://api.inworld.ai

# Inworld engine host used when signing JWT auth headers.
INWORLD_ENGINE_HOST=api-engine.inworld.ai

# Voice list cache TTL in milliseconds.
VOICE_CATALOG_CACHE_TTL_MS=300000

# Maximum number of curated English Inworld voices to import before appending Gemini legacy voices.
INWORLD_MAX_ENGLISH_VOICES=8

# Refresh JWT before it expires (milliseconds).
INWORLD_JWT_REFRESH_BUFFER_MS=60000

# Maximum combined prompt size before rejecting with 413.
AI_MAX_PROMPT_CHARS=8000

# Upstream AI request timeout in milliseconds (0 to disable).
AI_UPSTREAM_TIMEOUT_MS=30000

# ------------------------------
# LLM Provider Configuration
# ------------------------------

# "local" (llama.cpp/Ollama) or "gemini"
LLM_PROVIDER=local

# Local llama.cpp / Ollama endpoint
LOCAL_LLM_BASE_URL=http://127.0.0.1:8080
# Default: Qwen 2.5 32B Instruct Q4_K_M (~20GB VRAM).
LOCAL_LLM_MODEL=qwen2.5-32b-instruct
LOCAL_LLM_TEMPERATURE=0.7
LOCAL_LLM_TOP_P=0.9
LOCAL_LLM_MAX_TOKENS=4096
LOCAL_LLM_CONTEXT_WINDOW=32768

# Gemini text model defaults for /api/llm
GEMINI_MODEL=gemini-2.5-flash
GEMINI_TEMPERATURE=0.7
GEMINI_TOP_P=0.9
GEMINI_MAX_TOKENS=1024

# Generation budgets (tokens)
LLM_MAX_INPUT_TOKENS=24000
LLM_MAX_OUTPUT_TOKENS=1024
LLM_STATE_BUDGET=6000
LLM_BLOCKS_BUDGET=8000
LLM_SYSTEM_BUDGET=500
LLM_INSTRUCTION_BUDGET=300

# Prompt safety/dedupe
LLM_DEDUPE_WINDOW_MS=5000
LLM_TOKEN_SPIKE_MULT=1.5
