# Operation Catalog

## Operation Catalog (LLM Text Generation)

| File + function/component                                | Operation type         | Trigger path                                                                                          | Cancellation mechanism                                                                                                                        | Where result commits to state                                                                                                                                       |
| -------------------------------------------------------- | ---------------------- | ----------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `App.tsx:729` `requestTitleSuggestion`                   | `titleSuggestion`      | `generateScriptElement(...)` -> `services/ai.ts` -> `/api/ai/generate` (`kind=generateScriptElement`) | Token supersession only (`titleSuggestionTokenRef`); no request `cancel()` handle used                                                        | `setIsSuggestingTitle`, `setSuggestedTitle`, `setSuggestedTitleDismissed`, optional `setContext` title (`App.tsx:733`, `App.tsx:746`, `App.tsx:747`, `App.tsx:749`) |
| `App.tsx:764` `handleStart`                              | `generateOpeningScene` | `createGenerateSceneRequest(...)` -> `/api/ai/generate` (`kind=generateScene`)                        | `startAiRequest` cancels prior `activeAiRequestRef` (`App.tsx:409`); underlying `AbortController` in `createAiRequest` (`services/ai.ts:243`) | `setContext` initial + first scene (`App.tsx:798`), plus scroll tokens                                                                                              |
| `App.tsx:813` `handleGenerateNext`                       | `generateNextScene`    | `createGenerateSceneRequest(...)` -> `/api/ai/generate` (`kind=generateScene`)                        | Same as above (`startAiRequest` / `request.cancel`)                                                                                           | `setContext` append scene (`App.tsx:824`), `setUserInstruction('')` (`App.tsx:831`)                                                                                 |
| `App.tsx:841` `handleTwist`                              | `suggestPlotTwist`     | `createSuggestPlotTwistRequest(...)` -> `/api/ai/generate` (`kind=suggestPlotTwist`)                  | Same central cancellable request flow                                                                                                         | `setUserInstruction(\`PLOT TWIST: ...\`)` (`App.tsx:848`)                                                                                                           |
| `App.tsx:1032` `handleRegenerateBlock`                   | `rewriteBlock`         | `createRegenerateScriptBlockRequest(...)` -> `/api/ai/generate` (`kind=regenerateScriptBlock`)        | Same central cancellable request flow                                                                                                         | `setContext` updates block text (`App.tsx:1050`), undo toast closure can restore text (`App.tsx:1067`)                                                              |
| `components/InsertBlock.tsx:193` `handleSurpriseMe`      | `insertSurpriseText`   | `generateScriptElement(...)` -> `/api/ai/generate` (`kind=generateScriptElement`)                     | No abort/cancel; guarded by `isGenerating` flag                                                                                               | Local component state: `setContent(sanitized)` (`components/InsertBlock.tsx:208`)                                                                                   |
| `components/SetupForm.tsx:103` `handleSurpriseMe`        | `setupSurprise`        | `generateSurpriseSetup(...)` -> `/api/ai/generate` (`kind=generateSurpriseSetup`)                     | No abort/cancel; guarded by `isSurprising` flag                                                                                               | `updateValue(...)` (`components/SetupForm.tsx:112`) -> parent `onChange` -> `setSetupState` (`App.tsx:353`)                                                         |
| `components/SetupForm.tsx:152` auto-surprise `useEffect` | `setupAutoSurprise`    | Auto-calls `handleSurpriseMe()` (`components/SetupForm.tsx:159`)                                      | One-shot gate `autoSurpriseRef`; no network abort                                                                                             | Same commit path as manual setup surprise                                                                                                                           |
| `server/index.js:850` `handleAiGenerate` (text kinds)    | `backendTextDispatch`  | Route `/api/ai/generate` -> `generateTextByKind(...)` (`server/index.js:962`)                         | Server timeout wrapper (`withTimeout`) but no upstream abort propagation                                                                      | HTTP response only: `res.json({ data })` (`server/index.js:1002`)                                                                                                   |

---

## Operation Catalog (TTS Generation)

| File + function/component                                        | Operation type          | Trigger path                                                                                                                                                       | Cancellation mechanism                                                                                                                                                  | Where result commits to state                                                                                                                                                 |
| ---------------------------------------------------------------- | ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `App.tsx:1156` `handlePlay`                                      | `ttsPlaybackPrefetch`   | `playScript(allBlocks)` -> `hooks/useAudioPlayer.ts:369` -> `ScriptEngine.start` -> `fetchAudio` -> `createGenerateSpeechRequest` (`services/scriptEngine.ts:278`) | `playScript` calls `stop()` first (`hooks/useAudioPlayer.ts:378`); `ScriptEngine.stop` aborts inflight (`services/scriptEngine.ts:121`, `services/scriptEngine.ts:353`) | Hook state: `setBlockStatuses`/`setIsPlaying`/buffer progress (`hooks/useAudioPlayer.ts:385` etc.)                                                                            |
| `App.tsx:1358` playback `onRefreshAudio`                         | `ttsPlaybackRefresh`    | `playScript(..., { forceRegenerate: true })` same chain as above                                                                                                   | Same as above; forces non-reuse path                                                                                                                                    | Same hook playback state commits                                                                                                                                              |
| `App.tsx:1226` `handleModalPreview`                              | `ttsPreviewModal`       | `playPreview(text, config)` -> `engine.generateSingle(...)` (`hooks/useAudioPlayer.ts:547`) -> `createGenerateSpeechRequest`                                       | Calls `stop()` before preview (`App.tsx:1234`, `hooks/useAudioPlayer.ts:541`)                                                                                           | App state `setPreviewVoiceId` (`App.tsx:1236`); hook preview state `setIsLoadingAudio` / `setIsPreviewPlaying` (`hooks/useAudioPlayer.ts:542`, `hooks/useAudioPlayer.ts:576`) |
| `App.tsx:1332` `VoicesPanel` preview lambda                      | `ttsPreviewPanel`       | `onPreview={(config) => playPreview(...)}` same generateSingle chain                                                                                               | Same stop+abort behavior in hook                                                                                                                                        | Hook preview state commits                                                                                                                                                    |
| `hooks/useAudioPlayer.ts:479` `retryCurrentBlock`                | `ttsBlockRetry`         | `engine.generateSingle(block.text, voiceId, ...)` -> `ScriptEngine.fetchAudio` -> `createGenerateSpeechRequest`                                                    | No per-call cancel in handler; global `stop()` still aborts via engine                                                                                                  | Hook state `setBlockStatuses` generating/ready/error (`hooks/useAudioPlayer.ts:482`, `hooks/useAudioPlayer.ts:504`, `hooks/useAudioPlayer.ts:514`)                            |
| `services/scriptEngine.ts:278` private `fetchAudio`              | `ttsPerRequestInternal` | Internal request creator for both prefetch and preview/retry                                                                                                       | Tracks cancels in `inflightCancels`; aborted on engine stop                                                                                                             | Emits events (`audio`/`error`) to hook (`services/scriptEngine.ts:224`, `services/scriptEngine.ts:248`)                                                                       |
| `server/index.js:975` `handleAiGenerate` (`kind=generateSpeech`) | `backendTtsDispatch`    | Route `/api/ai/generate` -> `generateSpeechByProvider(...)` (`server/index.js:983`)                                                                                | Timeout wrapper only; no upstream abort propagation                                                                                                                     | HTTP response `res.json({ data: { audioBase64 } })` (`server/index.js:1002`)                                                                                                  |

---

## Completeness Verification (rg + TypeDoc reconciliation)

1. Runtime AI/TTS call-site scan (`rg`, non-test files) found these direct service triggers:
   - LLM: `App.tsx:741`, `App.tsx:789`, `App.tsx:819`, `App.tsx:845`, `App.tsx:1044`, `components/InsertBlock.tsx:200`, `components/SetupForm.tsx:111`
   - TTS: `services/scriptEngine.ts:278` (all block/preview/retry synthesis flows converge here)
2. TypeDoc symbol scan (`docs/typedoc/typedoc.json`) includes exported surfaces like:
   - `createGenerateSceneRequest`, `createSuggestPlotTwistRequest`, `createRegenerateScriptBlockRequest`, `generateScriptElement`, `generateSurpriseSetup`, `createGenerateSpeechRequest`, `useAudioPlayer`, `ScriptEngine`, `handleAiGenerate`, `generateTextByKind`

## Hidden call sites not visible as TypeDoc symbols

- Internal/local handlers driving real operations are not first-class TypeDoc symbols:
  - `App.tsx:729` `requestTitleSuggestion`
  - `App.tsx:764` `handleStart`
  - `App.tsx:813` `handleGenerateNext`
  - `App.tsx:841` `handleTwist`
  - `App.tsx:1032` `handleRegenerateBlock`
  - `components/InsertBlock.tsx:193` `handleSurpriseMe`
  - `components/SetupForm.tsx:103` `handleSurpriseMe` and auto-trigger at `components/SetupForm.tsx:159`
- Private internal TTS call site is hidden from exported docs:
  - `services/scriptEngine.ts:278` `fetchAudio` -> `createGenerateSpeechRequest(...)`
